# Bug Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all 44 bugs from bug-report-2026-04-02.md

**Architecture:** Bug fixes grouped by module layer, dispatched in parallel batches

**Tech Stack:** Python, PySide6, sqlite3, threading

---

## Batch 1: infrastructure/

- [ ] C-02: db_write_worker.py — add double-check lock to `_get_connection()`
- [ ] C-03: sqlite_manager.py — replace `_write_worker._conn` with `_write_worker._get_connection()` (7 sites)
- [ ] M-04: http_client.py — wrap content-length parse in try/except
- [ ] M-05: image_cache.py — catch FileNotFoundError in get(), cleanup()
- [ ] L-02: db_write_worker.py — log exception instead of `pass`
- [ ] L-03: pixmap_cache.py — add threading.Lock to initialize()
- [ ] L-04: font_loader.py — wrap addApplicationFont in try/except

## Batch 2: system/

- [ ] H-03: event_bus.py — add Lock to instance()
- [ ] H-03: application.py, bootstrap.py, theme.py — same singleton fix
- [ ] H-04: config.py — add RLock to cache get/set
- [ ] H-05: hotkeys.py — save listener to module variable
- [ ] M-06: mpris.py — guard start() with `if self._service is not None: return`
- [ ] M-07: mpris.py — add logger.warning to stop() except
- [ ] M-08: config.py — narrow base64 except to ValueError, binascii.Error

## Batch 3: services/

- [ ] C-04: download_service.py — move cache check and worker creation into lock
- [ ] H-01: sleep_timer_service.py — disconnect before reconnect signal
- [ ] H-02: library_service.py — stop timer before immediate refresh
- [ ] H-06: qqmusic/client.py — precompute start_idx, use index comparison
- [ ] H-07: qqmusic/qr_login.py — add len(data) checks before access

## Batch 4: repositories/

- [ ] M-01: album_repository.py — delete unreachable duplicate commit/return
- [ ] M-02: cloud_repository.py — wrap deletes in try/except with rollback
- [ ] M-03: playlist_repository.py — add `return False` after retry loop
- [ ] L-01: domain/playlist_item.py — remove unused logging import
- [ ] L-05: base_repository.py — add close() method for thread-local conn
- [ ] L-06: track_repository.py — add logger.error in except

## Batch 5: UI dialogs

- [ ] H-10: lyrics_download_dialog.py — remove terminate(), add longer wait
- [ ] H-11: qqmusic_qr_login_dialog.py — add wait(2000) after stop()
- [ ] M-09: organize_files_dialog.py — set parent=self on thread
- [ ] M-14: settings_dialog.py — set parent=self on VerifyLoginThread
- [ ] M-19: message_dialog.py — init self._result = StandardButton.Cancel

## Batch 6: UI views

- [ ] C-01: queue_view.py — replace eval() with ast.literal_eval()
- [ ] M-13: queue_view.py — verify selection adjustment logic
- [ ] H-08: online_grid_view.py — use QMetaObject.invokeMethod
- [ ] M-10: albums_view.py — store executor as instance var
- [ ] M-10: album_card.py — store executor as instance var
- [ ] M-11: local_tracks_list_view.py — use instance-level signal
- [ ] M-12: cloud_drive_view.py — disconnect EventBus in cleanup
- [ ] M-15: mini_player.py — store thread refs, cancel on close
- [ ] M-17: online_detail_view.py — check widget validity before signal

## Batch 7: UI widgets

- [ ] H-09: cover_controller.py — use Qt.QueuedConnection
- [ ] M-16: player_controls.py — disconnect signals in cleanup
- [ ] M-18: recommend_card.py — add requestInterruption before wait

## Batch 8: misc

- [ ] L-07: theme.py — change warning to error with exc_info
- [ ] L-08: application.py — only set _instance in create()
